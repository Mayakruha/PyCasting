# pyproject.toml
[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PyCasting"
version = "0.1.001"
dependencies = [
  "vtk",
  "FEMtoolkit @ git+https://github.com/mayakruha/FEMtoolkit.git",
  "meshio @ git+https://github.com/mayakruha/meshio.git",
]
